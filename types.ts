export interface EventInfo {
  name: string;
  startDate: string;
  endDate: string;
  location: string;
  locationUrl: string;
  price: number;
  batchInfo: string;
  registrationUrl: string;
  contactPhone: string;
}

export interface IconProps {
  className?: string;
  size?: number;
}