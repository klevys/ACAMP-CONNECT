export interface EventInfo {
  name: string;
  startDate: string;
  endDate: string;
  location: string;
  locationUrl: string;
  fullAddress: string;
  price: number;
  batchInfo: string;
  registrationUrl: string;
  contactPhone: string;
  contactDisplay: string;
}

export interface IconProps {
  className?: string;
  size?: number;
}